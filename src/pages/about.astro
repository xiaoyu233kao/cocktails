---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';

const entries = await getCollection('cocktails');
const entryCount = entries.length;
const communityCategories = new Set(['Bar Assistant Community', 'Curated Community']);
const ibaCount = entries.filter((entry) => !communityCategories.has(entry.data.category)).length;
const communityCount = entries.filter((entry) => communityCategories.has(entry.data.category)).length;
---
<BaseLayout title="关于这个图鉴｜Cocktail Atlas" description={`Cocktail Atlas 收录 ${entryCount} 款鸡尾酒，其中 ${ibaCount} 款来自 IBA，另有 ${communityCount} 款精选社区补充配方。`}>
  <section class="catalog-hero">
    <div class="shell">
      <span class="eyebrow">ABOUT THE ATLAS</span>
      <h1>关于这个图鉴</h1>
      <p>这里收录 {entryCount} 款鸡尾酒，其中 IBA 当前目录 {ibaCount} 款，并补充 {communityCount} 款精选社区配方；中文为主，保留中英文名称和来源。</p>
    </div>
  </section>
  <section class="section section--tight">
    <div class="shell detail-content">
      <div class="detail-main">
        <section>
          <h2>内容原则</h2>
          <p class="story">主配方优先参考 International Bartenders Association（IBA）当前目录；社区补充配方来自固定版本的 Bar Assistant public data 快照精选清单，另保留一条有明确来源的手工补录。步骤用原创中文描述，原料用量保留来源中的单位与文字。故事和变体只有在能逐条附来源时才会出现。</p>
        </section>
        <section>
          <h2>图片记录</h2>
          <p class="story">当前目录的图片均按生成式示意图记录：IBA 条目使用本地 WebP，精选社区条目统一预留本地 WebP 路径，并记录 OpenAI image generation 的生成 credit。社区图片补齐前，内容校验会明确列出待补文件；页面不会将其描述为真实成品。若未来采用授权照片，仍需记录作者、来源、许可和必要署名。</p>
        </section>
      </div>
      <aside class="detail-aside">
        <section>
          <h2>维护入口</h2>
          <p class="story">新增一杯酒时，按仓库里的 CONTENT-SCHEMA.md 创建同名 Markdown 文件与同系列示意图，并在提交前运行内容校验和生产构建。Bar Assistant 数据快照按 MIT 许可随仓库保存，公开目录只发布 curated allowlist 中的 38 条，源图片不复制；导入清单记录提交、校验和与原始来源。</p>
        </section>
      </aside>
    </div>
  </section>
</BaseLayout>
